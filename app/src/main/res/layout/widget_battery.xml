<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:gravity="center"
    android:padding="8dp">

    <!-- Indicador pequeño superior (decorativo/moderno) -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BATERÍA"
        android:textSize="12sp"
        android:letterSpacing="0.2"
        android:textColor="#A0A0A0"
        android:layout_marginBottom="4dp"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center">
        
        <ImageView
            android:id="@+id/battery_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/ic_battery_80" />

        <!-- Porcentaje gigante con AutoSize para que se adapte al estirar el widget (API 26+) -->
        <TextView
            android:id="@+id/battery_percentage_text"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:text="--%"
            android:textSize="56sp"
            android:autoSizeTextType="uniform"
            android:autoSizeMinTextSize="24sp"
            android:autoSizeMaxTextSize="96sp"
            android:fontFamily="sans-serif-medium"
            android:textColor="#FFFFFF"
            android:includeFontPadding="false"
            android:shadowColor="#33000000"
            android:shadowDx="0"
            android:shadowDy="4"
            android:shadowRadius="8"/>
    </LinearLayout>

    <!-- Estado (Cargando/Desconectado) con estilo más sutil -->
    <TextView
        android:id="@+id/battery_status_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="14sp"
        android:fontFamily="sans-serif"
        android:textColor="#A0A0A5"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="4dp"/>

</LinearLayout>
